{
  "name": "cra",
  "version": "1.0.0",
  "description": "",
  "main": "./server/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server/index.js",
    "build": "cd client && npm install && npm run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^5.7.0",
    "unirest": "^0.6.0"
  },
  "engines": {
    "node": "18.0.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
